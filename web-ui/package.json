{
  "name": "plaintext-tank-ui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test:protocol": "tsc --target ES2020 --module commonjs --moduleResolution node --strict --esModuleInterop --skipLibCheck --outDir ../build-web-tests src/protocolPayload.ts tests/protocolPayloadSelftest.ts && node ../build-web-tests/tests/protocolPayloadSelftest.js",
    "verify": "npm run test:protocol && npm run build"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.0.0",
    "vite": "^6.0.0"
  }
}
