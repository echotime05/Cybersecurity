# Useless / Local Artifacts

This folder is for local, non-source artifacts that are not part of the submitted course-design code.

Examples:

- old generated Visual Studio build folders
- temporary redirected stdout/stderr files
- stale logs that should not live in the project root

The actual artifacts under `useless/local-artifacts/` are ignored by Git. They can be deleted or regenerated at any time.
