# 当前系统设计参考稿

> 本文档用于辅助撰写最终课程设计报告，不作为直接提交的排版版报告。旧 Word 文档只作为章节结构和图片素材参考；当前实现以代码、README、测试和最近提交为准。

## 1. 文档定位与当前版本

本文档记录当前已经跑通并推送到 GitHub 的 Cyber Tank Battle 系统设计。目标不是替代最终报告，而是提供一份足够细的事实底稿：写报告时可以直接回看本文档，而不必反复回到 C++ 源码中查字段顺序、加密边界、线程模型和日志路径。

当前项目是一个 Windows C++17 Kerberos-style tank battle demo with browser UI。最终公开运行模式是 `--game-auth-encrypted`。该模式包含完整 AS/TGS/V 认证、Client/V 证书交换、Client/V 游戏 `MSG_APP` payload 签名后加密，以及每个非 ACK 游戏报文的签名加密 `APP_ACK` 双向不可否认证据。

旧 Word 文档仍可作为目录结构和图片素材来源，但旧文档不是当前行为的事实来源。旧文档中关于明文应用层、浏览器直连 V、Client 直接读取 AS/TGS/V 普通日志、等待所有 Client 后统一 `GAME_START` 等内容，均不代表当前最终实现。

### 当前事实来源

| 来源 | 用途 |
| --- | --- |
| `README.md` | 运行方式、部署方式、日志路径、性能测试入口 |
| `include/cyber/common/types.hpp` | 实体 ID、消息类型、错误码、应用码 |
| `include/cyber/common/packet.hpp` / `src/common/packet.cpp` | 统一 Packet 首部、序列化和错误/app 简单封装 |
| `include/cyber/common/protocol_payloads.hpp` / `src/common/protocol_payloads.cpp` | Kerberos、证书、`APP_ACK`、`SignedAppPayload` 字段顺序 |
| `include/cyber/game/game_protocol.hpp` / `src/game/game_protocol.cpp` | 坦克大战应用层 `GameMessage` 和状态快照结构 |
| `include/cyber/game/game_non_repudiation.hpp` / `src/game/game_non_repudiation.cpp` | 签名加密游戏报文与 ACK 证据 |
| `src/common/auth_flow.cpp` | AS/TGS/V_AUTH/CERT 认证链路 |
| `src/game/tank_game_client.cpp` | Client 登录、WebSocket bridge、V 接收循环、ACK 发送 |
| `src/game/tank_game_server.cpp` | V 长连接、认证、游戏输入处理、状态广播、ACK 处理 |
| `src/common/protocol_event.cpp` / `web-ui/src/protocolPayload.ts` | 协议事件日志与 Protocol UI 结构化解析 |

## 2. 系统总体架构

当前系统的主链路是：

```text
Browser UI <-> local client.exe WebSocket bridge <-> AS/TGS/V over TCP
Protocol panel <-> local monitor.exe WebSocket bridge <-> local packet logs
```

浏览器 UI 不直接连接 V。浏览器只连接本机 `client.exe` 暴露的 WebSocket，例如 `ws://127.0.0.1:7001`。`client.exe` 负责把浏览器输入转换为 C++ TCP 协议报文，并负责认证、加密、签名、ACK 和状态转发。

Protocol 面板不直接读取 AS/TGS/V 普通日志。它只连接本机 `monitor.exe`，例如 `ws://127.0.0.1:7010`。`monitor.exe` 读取本机 `logs/protocol_events/*.txt`，把结构化后的 `protocolEvent` JSON 推给浏览器。因此每台物理机只展示本机正在运行的进程的发包和收包。

### 端到端数据流

1. 用户在浏览器选择 Client ID 并输入真实密码。
2. 浏览器通过本机 WebSocket 把登录命令发送给 `client.exe`。
3. `client.exe` 依次与 AS、TGS、V 建立 TCP 通信，完成 Kerberos 风格认证。
4. `client.exe` 与 V 在同一条 V TCP 连接上完成 `CERT_C2V` / `CERT_V2C`。
5. 登录认证成功后，浏览器显示加入游戏入口。
6. 用户输入移动、瞄准、开火等操作，浏览器把输入发给本机 `client.exe`。
7. `client.exe` 将输入封装为 `MSG_APP`，对应用层内容签名，然后用 `Kc_v` 加密 payload，发送给 V。
8. V 验签和解密成功后更新权威 `BattleRoom`，并给该非 ACK 报文返回签名加密 `APP_ACK`。
9. V 每 20ms tick 更新世界并广播 `GAME_STATE`。
10. Client 收到 V 的 `GAME_STATE` 后验签、解密、回 ACK，并把权威状态转成 UI JSON 发给浏览器渲染。

## 3. 角色职责

### AS

AS 是认证服务器。它处理 `MSG_AS_REQ`，根据请求中的 `idc` 找到 Client 的口令派生密钥 `Kc`，生成 `Kc_tgs` 和 `Ticket_tgs`。AS 返回的 `MSG_AS_REP` 整体用 `Kc` 加密，其中内嵌的 `ticket_tgs` 再用 `KTGS` 加密。

### TGS

TGS 是票据授予服务器。它处理 `MSG_TGS_REQ`，解开 `Ticket_tgs` 和 `authenticator_tgs`，校验 Client 身份、TGS 身份、目标服务 V 和时间戳，然后生成 `Kc_v` 与 `Ticket_v`。TGS 返回的 `MSG_TGS_REP` 整体用 `Kc_tgs` 加密，其中内嵌的 `ticket_v` 用 `KV` 加密。

### V

V 是应用服务器，也是坦克大战权威世界状态拥有者。V 先处理 `MSG_V_AUTH_REQ`，解开 `Ticket_v` 和 `authenticator_v`，校验 Client 身份、V 身份和时间戳。之后 V 处理 Client/V 证书交换，保存 Client 公钥。认证完成后，V 保持该 TCP socket，继续承载游戏 `MSG_APP`。

### Client

Client 是本机浏览器与 C++ 安全游戏链路之间的代理。它负责真实密码登录、密钥派生、AS/TGS/V_AUTH/CERT 完整流程、游戏输入上报、权威状态接收、签名加密、ACK 发送和 UI 状态广播。浏览器只面对本地 Client bridge，不直接接触 AS/TGS/V 的 TCP 协议。

### monitor.exe

monitor.exe 是协议事件可视化桥。它不参与安全协议和游戏逻辑，只读取本机 `logs/protocol_events/*.txt` 中的 `PACKET_SEND` / `PACKET_RECV` 事件，去重、排序并转换为 `protocolEvent` JSON 发给浏览器。

## 4. 运行模式与部署方式

当前推荐运行模式如下：

```text
as_server.exe --serve
tgs_server.exe --serve
v_server.exe --game-auth-encrypted
client.exe --game-auth-encrypted --ui-port 7001
monitor.exe --ui-port 7010
```

单机演示使用 `scripts/run_local.ps1` 启动 AS、TGS、V、Client 和 Monitor。脚本会构建需要的目标，停止旧本地角色进程，并默认清理根目录普通日志、`logs/runtime` 与 `logs/protocol_events` 下的旧运行输出，使 Protocol 面板展示当前运行产生的包。

Web UI 使用 `scripts/run_web.ps1` 启动，默认地址是：

```text
http://127.0.0.1:5173/?client=ws://127.0.0.1:7001&monitor=ws://127.0.0.1:7010
```

四机部署时，AS、TGS、V 和所有 Client 使用一致的 `AS_IP`、`TGS_IP`、`V_IP` 与端口配置。每个玩家所在机器都启动自己的 `client.exe` 和 `monitor.exe`，浏览器仍然连接本机 `127.0.0.1` 上的 Client bridge 和 Monitor bridge。AS/TGS/V-only 主机如果也要查看本机协议包，则单独启动本机 `monitor.exe`。

与旧文档不同，当前浏览器 UI 不直接连接 V，也不由 Client 直接读取 AS/TGS/V 的普通日志。浏览器只连接本地 `client.exe` 和本地 `monitor.exe`；`monitor.exe` 只展示本机进程写出的协议事件日志。

## 5. 配置、实体 ID 与消息类型

### 实体 ID

| 角色 | ID | 说明 |
| --- | --- | --- |
| Client1 | `0x01` | 玩家客户端 1 |
| Client2 | `0x02` | 玩家客户端 2 |
| Client3 | `0x03` | 玩家客户端 3 |
| Client4 | `0x04` | 玩家客户端 4 |
| AS | `0x11` | Authentication Server |
| TGS | `0x12` | Ticket Granting Server |
| V | `0x13` | 游戏应用服务器 |
| Unknown | `0xFF` | 内部默认未知值，不作为正常网络角色 |

### MsgType

| MsgType | 数值 | 方向 | 用途 |
| --- | --- | --- | --- |
| `MSG_AS_REQ` | `0x01` | Client -> AS | 请求 TGS 票据，payload 明文 |
| `MSG_AS_REP` | `0x02` | AS -> Client | 返回 `Kc_tgs` 和 `Ticket_tgs`，payload 用 `Kc` 加密 |
| `MSG_TGS_REQ` | `0x03` | Client -> TGS | 请求 V 票据，包含加密票据和认证器 |
| `MSG_TGS_REP` | `0x04` | TGS -> Client | 返回 `Kc_v` 和 `Ticket_v`，payload 用 `Kc_tgs` 加密 |
| `MSG_V_AUTH_REQ` | `0x05` | Client -> V | 向 V 证明持有 `Ticket_v` 和 `Kc_v` |
| `MSG_V_AUTH_REP` | `0x06` | V -> Client | 返回 `ts5+1`，payload 用 `Kc_v` 加密 |
| `MSG_CERT_C2V` | `0x07` | Client -> V | Client 证书，payload 用 `Kc_v` 加密 |
| `MSG_CERT_V2C` | `0x08` | V -> Client | V 证书，payload 用 `Kc_v` 加密 |
| `MSG_ERROR` | `0x65` | 任意方向 | 错误码和错误文本 |
| `MSG_APP` | `0x66` | Client <-> V | 游戏应用层报文和 `APP_ACK` |

### ErrorCode

| ErrorCode | 数值 | 说明 |
| --- | --- | --- |
| `ERR_PASSWORD_WRONG` | `0x01` | 密码错误或无法得到合法 AS 响应 |
| `ERR_TGT_EXPIRED` | `0x02` | TGS 票据过期 |
| `ERR_TICKET_V_EXPIRED` | `0x03` | V 票据过期 |
| `ERR_TGS_ID_MISMATCH` | `0x04` | TGS 身份不匹配 |
| `ERR_V_ID_MISMATCH` | `0x05` | V 身份不匹配 |
| `ERR_REPLAY_DETECTED` | `0x06` | 重放检测失败 |
| `ERR_UNSUPPORTED_MSG_TYPE` | `0x07` | 收到不支持的消息类型 |

`MSG_ERROR` payload 结构为：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `err_code` | `u8` | 上表中的错误码 |
| `err_msg` | raw bytes | 剩余字节作为错误文本 |

### AppCode

| AppCode | 数值 | 当前用途 |
| --- | --- | --- |
| `KEY_DOWN` | `0x01` | 旧输入事件枚举，当前最终游戏链路不以它作为主协议 |
| `KEY_UP` | `0x02` | 旧输入事件枚举，当前最终游戏链路不以它作为主协议 |
| `AIM_EVENT` | `0x03` | 旧瞄准事件枚举，当前最终游戏链路不以它作为主协议 |
| `FIRE_EVENT` | `0x04` | 旧开火事件枚举，当前最终游戏链路不以它作为主协议 |
| `GAME_JOIN_REQ` | `0x05` | 当前加入游戏请求 |
| `GAME_START` | `0x06` | 保留枚举，当前最终行为不依赖等待全员后统一 start |
| `GAME_STATE` | `0x07` | V -> Client 权威世界状态 |
| `APP_ACK` | `0x08` | 双向不可否认证据 ACK |
| `GAME_MOVE` | `0x09` | Client -> V 移动输入 |
| `GAME_TARGET` | `0x0A` | Client -> V 炮塔角度 |
| `GAME_SHOOT` | `0x0B` | Client -> V 开火状态 |
| `GAME_NAME` | `0x0C` | Client -> V 玩家名更新 |

## 6. 通用报文格式

### 基础编码规则

| 记法 | 字节数 | 编码规则 |
| --- | --- | --- |
| `u8` | 1 | 无符号 8 位整数，直接写入 1 字节 |
| `i8` | 1 | 有符号 8 位值，按原始字节写入，解析时转回 `int8_t` |
| `u16` | 2 | 大端序，先高字节后低字节 |
| `u32` | 4 | 大端序 |
| `u64` | 8 | 大端序 |
| `f32` | 4 | IEEE-754 float32 的 bit pattern 作为大端 `u32` 写入 |
| `bool_u8` | 1 | `0` 表示 false，非 0 表示 true；当前构造函数写 `0` 或 `1` |
| `string_u8` | 变长 | `u8 length` + 原始字符串字节，最大 255 字节 |
| `bytes_u16` | 变长 | `u16 length` + 原始字节，最大 65535 字节 |

Kerberos payload、游戏 payload 和通用 Packet header 都使用这些规则。所有多字节整数均为大端序。

### 统一 Packet Header

所有 TCP 业务报文使用统一固定首部加变长 payload 的格式。固定首部长度为 11 bytes：

| 偏移 | 字段 | 长度 | 说明 |
| --- | --- | --- | --- |
| `0` | `msg_type` | 1B | 报文类型，例如 `MSG_AS_REQ`、`MSG_APP` |
| `1` | `src` | 1B | 源实体 ID |
| `2` | `dst` | 1B | 目的实体 ID |
| `3` | `payload_len` | 4B | payload 字节长度，序列化时由 `payload.size()` 写入 |
| `7` | `reserved` | 4B | 保留字段，当前默认值为 `0` |
| `11` | `payload` | variable | 变长数据部分 |

加密只作用于 payload 字节，不改变固定首部。固定首部保持明文，是为了让网络收发、日志、路由和 Protocol 面板能够解析 `msg_type`、`src`、`dst`、`payload_len` 等基础信息。

### 两类 MSG_APP 封装

源码中有一个简单 helper：`make_app_payload(AppCode code, Bytes app_payload)`，它的结构是 `app_code u8 + app_payload`。当前最终 `--game-auth-encrypted` 游戏链路不直接使用这个简单结构作为 wire payload，而是使用更完整的 `SignedAppPayload`：

```text
Packet.msg_type = MSG_APP
Packet.payload = DES(Kc_v, SignedAppPayload)
```

`SignedAppPayload` 自身第一个字段仍然是 `app_code`，因此 Protocol UI 解密后仍能展示 `MSG_APP.GAME_MOVE`、`MSG_APP.GAME_STATE`、`MSG_APP.APP_ACK` 等后缀。

## 7. Kerberos 认证流程

### 总体链路

Client 登录时使用真实输入密码。`client.exe` 根据 Client ID 和密码派生 `Kc`，然后发送 `MSG_AS_REQ` 到 AS。AS 按配置中对应 Client 的密码派生同一个 `Kc`，生成 `Kc_tgs` 与 `Ticket_tgs`，并把 `AS_REP` 主体用 `Kc` 加密返回。密码错误时，Client 无法用本地派生的 `Kc` 解出合法 `AS_REP`，登录失败。

之后 Client 使用 `Kc_tgs` 向 TGS 申请 V 的服务票据，得到 `Kc_v` 和 `Ticket_v`。最后 Client 使用 `Ticket_v` 和 `Kc_v` 向 V 做服务认证。当前实现中，V_AUTH 完成后 TCP socket 不关闭；该连接继续承载证书交换和游戏应用层。

### AS_REQ

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_AS_REQ`, `src=ClientX`, `dst=AS` |
| Payload 加密 | 不加密 |
| 语义 | Client 请求访问 TGS |

Payload 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `idc` | `u8` | Client ID |
| 2 | `idtgs` | `u8` | 目标 TGS ID，当前为 `0x12` |
| 3 | `ts1` | `u64` | Client 发起认证的时间戳 |

`AS_REQ` payload 固定为 10 bytes。

### Ticket_tgs

`Ticket_tgs` 是 AS 生成并交给 TGS 验证的票据。它的明文结构如下，但 wire 中的 `ticket_tgs` 字段是 `DES(KTGS, Ticket_tgs plain body)`。

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `kc_tgs` | `u64` | Client 与 TGS 的会话密钥 |
| 2 | `idc` | `u8` | Client ID |
| 3 | `adc` | `u32` | Client 地址标识，当前默认 `0x7F000001` |
| 4 | `idtgs` | `u8` | TGS ID |
| 5 | `ts2` | `u64` | AS 生成票据的时间戳 |
| 6 | `lifetime2` | `u64` | TGS 票据有效期 |

### AS_REP

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_AS_REP`, `src=AS`, `dst=ClientX` |
| 外层 Payload 加密 | `DES(Kc, AS_REP plain body)` |
| 内嵌加密字段 | `ticket_tgs = DES(KTGS, Ticket_tgs plain body)` |

AS_REP 明文 body 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `kc_tgs` | `u64` | Client/TGS 会话密钥 |
| 2 | `idtgs` | `u8` | TGS ID |
| 3 | `ts2` | `u64` | AS 时间戳 |
| 4 | `lifetime2` | `u64` | TGS 票据有效期 |
| 5 | `ticket_tgs` | `bytes_u16` | 加密后的 TGS 票据 |

Client 用 `Kc` 解开 AS_REP 后得到 `Kc_tgs` 和 `ticket_tgs`。Client 不解开 `ticket_tgs`，它只把该字段转交给 TGS。

### Authenticator

TGS 阶段和 V_AUTH 阶段都使用同一种认证器明文结构：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `idc` | `u8` | Client ID |
| 2 | `adc` | `u32` | Client 地址标识 |
| 3 | `ts` | `u64` | 本次认证器时间戳 |

`authenticator_tgs` 使用 `Kc_tgs` 加密，`authenticator_v` 使用 `Kc_v` 加密。

### TGS_REQ

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_TGS_REQ`, `src=ClientX`, `dst=TGS` |
| Payload 整体加密 | 不整体加密 |
| 内嵌加密字段 | `ticket_tgs`、`authenticator_tgs` |

Payload 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `idv` | `u8` | 目标服务 ID，当前为 V |
| 2 | `ticket_tgs` | `bytes_u16` | AS 发给 Client 的 TGS 票据，TGS 用 `KTGS` 解密 |
| 3 | `authenticator_tgs` | `bytes_u16` | Client 用 `Kc_tgs` 加密的认证器 |

TGS 解开 `ticket_tgs` 后取得 `Kc_tgs`，再用 `Kc_tgs` 解开 `authenticator_tgs`。TGS 校验 `ticket.idc == authenticator.idc`、`ticket.idtgs == TGS`、请求的 `idv == V`。

### Ticket_v

`Ticket_v` 是 TGS 生成并交给 V 验证的票据。它的明文结构如下，但 wire 中的 `ticket_v` 字段是 `DES(KV, Ticket_v plain body)`。

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `kc_v` | `u64` | Client 与 V 的会话密钥 |
| 2 | `idc` | `u8` | Client ID |
| 3 | `adc` | `u32` | Client 地址标识 |
| 4 | `idv` | `u8` | V ID |
| 5 | `ts4` | `u64` | TGS 生成票据的时间戳 |
| 6 | `lifetime4` | `u64` | V 票据有效期 |

### TGS_REP

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_TGS_REP`, `src=TGS`, `dst=ClientX` |
| 外层 Payload 加密 | `DES(Kc_tgs, TGS_REP plain body)` |
| 内嵌加密字段 | `ticket_v = DES(KV, Ticket_v plain body)` |

TGS_REP 明文 body 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `kc_v` | `u64` | Client/V 会话密钥 |
| 2 | `idv` | `u8` | V ID |
| 3 | `ts4` | `u64` | TGS 时间戳 |
| 4 | `ticket_v` | `bytes_u16` | 加密后的 V 票据 |

Client 用 `Kc_tgs` 解开 TGS_REP 后得到 `Kc_v` 和 `ticket_v`。Client 不解开 `ticket_v`，它只把该字段转交给 V。

### V_AUTH_REQ

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_V_AUTH_REQ`, `src=ClientX`, `dst=V` |
| Payload 整体加密 | 不整体加密 |
| 内嵌加密字段 | `ticket_v`、`authenticator_v` |

Payload 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `ticket_v` | `bytes_u16` | TGS 发给 Client 的 V 票据，V 用 `KV` 解密 |
| 2 | `authenticator_v` | `bytes_u16` | Client 用 `Kc_v` 加密的认证器 |

V 解开 `ticket_v` 得到 `Kc_v`，再用 `Kc_v` 解开 `authenticator_v`。V 校验 Client ID、V ID、地址标识和时间戳后建立认证会话。

### V_AUTH_REP

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_V_AUTH_REP`, `src=V`, `dst=ClientX` |
| Payload 加密 | `DES(Kc_v, V_AUTH_REP plain body)` |

V_AUTH_REP 明文 body 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `ts5_plus_1` | `u64` | V 对 Client 认证器时间戳的响应，Client 校验其为 `ts5 + 1` |

## 8. 证书交换流程

V_AUTH 成功后，Client 与 V 进行运行时证书交换。该流程的目的不是重新分发 `Kc_v`，而是让双方获得对方用于应用层签名验证的 RSA 公钥。

### 证书结构

证书序列化结构为：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `subject_id` | `u8` | 证书主体实体 ID |
| 2 | `subject_pk` | `bytes_u16` | 主体公钥，当前固定 36 bytes |
| 3 | `ca_signature` | `bytes_u16` | CA 对 `subject_id + subject_pk` 的签名 |

公钥 `subject_pk` 的结构为：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `n` | 32B | RSA 模数，以定长 32 字节整数编码 |
| 2 | `e` | `u32` | RSA 公钥指数 |

CA 签名的输入是 `hash64(certificate_material(subject_id, subject_pk))`。验证时使用配置中的 CA 公钥。

### CERT_C2V

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_CERT_C2V`, `src=ClientX`, `dst=V` |
| Payload 加密 | `DES(Kc_v, CERT_C2V plain body)` |

CERT_C2V 明文 body 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `client_id` | `u8` | Client ID |
| 2 | `cert` | `bytes_u16` | Client 证书 |

V 解密后校验 `client_id` 与 packet `src` 一致，并验证 Client 证书的 CA 签名。验证通过后，V 保存 Client 公钥，用于后续验证 Client 发送的 `MSG_APP` 签名。

### CERT_V2C

| 属性 | 内容 |
| --- | --- |
| Header | `msg_type=MSG_CERT_V2C`, `src=V`, `dst=ClientX` |
| Payload 加密 | `DES(Kc_v, CERT_V2C plain body)` |

CERT_V2C 明文 body 字段：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `v_id` | `u8` | V ID |
| 2 | `cert` | `bytes_u16` | V 证书 |

Client 解密后校验 `v_id == V`，并验证 V 证书的 CA 签名。验证通过后，Client 保存 V 公钥，用于后续验证 V 发送的 `GAME_STATE` 和 `APP_ACK`。

## 9. 加密、签名与消息认证

### 加密边界

当前实现的加密边界是 payload。`src/game/app_payload_codec.cpp` 中的 codec 只包装 `Packet.payload`，不改变 `msg_type`、`src`、`dst`、`payload_len` 和 `reserved`。Header 保持明文，Payload 按阶段密钥加密。

| 阶段 | 外层 payload 是否加密 | 密钥 |
| --- | --- | --- |
| `AS_REQ` | 否 | 无 |
| `AS_REP` | 是 | `Kc` |
| `TGS_REQ` | 否，内嵌字段加密 | `ticket_tgs` 用 `KTGS`，`authenticator_tgs` 用 `Kc_tgs` |
| `TGS_REP` | 是 | `Kc_tgs` |
| `V_AUTH_REQ` | 否，内嵌字段加密 | `ticket_v` 用 `KV`，`authenticator_v` 用 `Kc_v` |
| `V_AUTH_REP` | 是 | `Kc_v` |
| `CERT_C2V` / `CERT_V2C` | 是 | `Kc_v` |
| 游戏 `MSG_APP` | 是 | `Kc_v` |

DES-style 加密按 8 字节 block 处理，并使用类似 PKCS#7 的 padding。密文长度总是 8 的倍数。

### SignedAppPayload

应用层游戏报文采用“先签名、后加密”：

```text
GameMessage -> app_code + app_payload -> hash64 -> RSA-style signature
SignedAppPayload(app_code, app_payload, signature) -> DES-style encrypt with Kc_v
```

`SignedAppPayload` 明文结构为：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `app_code` | `u8` | `GAME_MOVE`、`GAME_STATE`、`APP_ACK` 等 |
| 2 | `app_payload` | `bytes_u16` | 对普通游戏包来说是 `GameMessage`；对 ACK 来说是 `AppAckPayload` |
| 3 | `signature` | `bytes_u16` | RSA-style 签名结果 |

签名输入是：

```text
hash64(app_code + app_payload)
```

也就是说，签名覆盖应用码和应用 payload，不覆盖 Packet header。`Packet.msg_type`、`src`、`dst`、`payload_len` 和 `reserved` 不属于 RSA 签名输入。

### GameMessage 与 AppCode 的对应关系

普通游戏消息的 `SignedAppPayload.app_payload` 是 `GameMessage`，其结构为：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `game_msg_type` | `u8` | `JOIN`、`MOVE`、`TARGET`、`SHOOT`、`NAME`、`STATE` |
| 2 | `game_payload` | variable | 对应游戏消息的具体字段 |

映射关系如下：

| GameMsgType | 数值 | AppCode | 方向 |
| --- | --- | --- | --- |
| `join` | `0x01` | `GAME_JOIN_REQ` | Client -> V |
| `move` | `0x02` | `GAME_MOVE` | Client -> V |
| `target` | `0x03` | `GAME_TARGET` | Client -> V |
| `shoot` | `0x04` | `GAME_SHOOT` | Client -> V |
| `name` | `0x05` | `GAME_NAME` | Client -> V |
| `state` | `0x10` | `GAME_STATE` | V -> Client |

接收方验签后还会校验 `app_code_for_game_message_type(game_msg_type) == signed_payload.app_code`。这可以防止外层 AppCode 与内层 GameMsgType 不一致。

## 10. 双向不可否认与 APP_ACK

当前双向不可否认覆盖 Client 与 V 之间的游戏应用层报文。每个非 ACK 的游戏 `MSG_APP` 报文在接收方校验成功后，接收方都会返回一个 `APP_ACK`。`APP_ACK` 本身也是 `MSG_APP`，同样签名后用 `Kc_v` 加密。

### AppAckPayload

`APP_ACK` 的 `SignedAppPayload.app_code = APP_ACK`，其 `app_payload` 是 `AppAckPayload`：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `acked_msg_type` | `u8` | 被确认报文的 `MsgType`，当前游戏 ACK 通常为 `MSG_APP` |
| 2 | `acked_app_code` | `u8` | 被确认报文解密验签后的 AppCode |
| 3 | `acked_src` | `u8` | 被确认报文的 header `src` |
| 4 | `acked_dst` | `u8` | 被确认报文的 header `dst` |
| 5 | `acked_payload_len` | `u32` | 被确认报文 wire payload 长度，即 `received_packet.payload.size()` |
| 6 | `acked_payload_hash` | `u64` | `hash64(received_packet.payload)` |

这里引用的是收到时网络上的 wire payload，也就是加密后的 `Packet.payload`。因此 ACK 证据绑定了原始密文 payload 的长度和 hash，而不是只绑定解密后的业务内容。

### ACK 规则

1. Client -> V 的 `GAME_JOIN_REQ`、`GAME_MOVE`、`GAME_TARGET`、`GAME_SHOOT`、`GAME_NAME` 都需要 V 返回 `APP_ACK`。
2. V -> Client 的 `GAME_STATE` 需要 Client 返回 `APP_ACK`。
3. `APP_ACK` 自身不再被 ACK，避免无限递归。
4. 只有签名验证通过、AppCode 和 GameMsgType 匹配的非 ACK 报文才会触发 ACK。
5. 收到并验证 ACK 后，把完整 ACK packet 写入证据日志。

证据日志路径：

```text
logs/v_ack.log
logs/client_ack.log
```

证据日志核心字段：

```text
APP_NON_REPUDIATION_ACK packet_hex=<complete ack packet hex>
```

`packet_hex` 是完整 ACK packet 的十六进制，包含 11B header 和加密后的签名 payload。

## 11. 坦克大战应用层设计

### 应用层总封装

当前最终游戏报文的 wire 结构为：

```text
Packet {
  msg_type = MSG_APP
  src = ClientX 或 V
  dst = V 或 ClientX
  payload = DES(Kc_v, SignedAppPayload)
}
```

普通游戏包内部是：

```text
SignedAppPayload {
  app_code = GAME_MOVE / GAME_STATE / ...
  app_payload = GameMessage
  signature = Sign(hash64(app_code + GameMessage))
}
```

ACK 包内部是：

```text
SignedAppPayload {
  app_code = APP_ACK
  app_payload = AppAckPayload
  signature = Sign(hash64(APP_ACK + AppAckPayload))
}
```

### 游戏 AppCode 到 Payload 的完整表

| AppCode | GameMsgType | 方向 | 是否加密 | 是否触发 ACK | Payload |
| --- | --- | --- | --- | --- | --- |
| `GAME_JOIN_REQ` (`0x05`) | `join` (`0x01`) | Client -> V | 是，`Kc_v` | 是 | `GameMessage(join, JoinMessage)` |
| `GAME_MOVE` (`0x09`) | `move` (`0x02`) | Client -> V | 是，`Kc_v` | 是 | `GameMessage(move, MoveMessage)` |
| `GAME_TARGET` (`0x0A`) | `target` (`0x03`) | Client -> V | 是，`Kc_v` | 是 | `GameMessage(target, TargetMessage)` |
| `GAME_SHOOT` (`0x0B`) | `shoot` (`0x04`) | Client -> V | 是，`Kc_v` | 是 | `GameMessage(shoot, ShootMessage)` |
| `GAME_NAME` (`0x0C`) | `name` (`0x05`) | Client -> V | 是，`Kc_v` | 是 | `GameMessage(name, NameMessage)` |
| `GAME_STATE` (`0x07`) | `state` (`0x10`) | V -> Client | 是，`Kc_v` | 是 | `GameMessage(state, BattleStateSnapshot)` |
| `APP_ACK` (`0x08`) | 无 | 双向 | 是，`Kc_v` | 否 | `AppAckPayload` |

### JoinMessage

`GAME_JOIN_REQ` 的 `game_payload`：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `client_id` | `u8` | 加入游戏的 Client ID |
| 2 | `name` | `string_u8` | 玩家名 |

V 收到后会校验 `JoinMessage.client_id` 与 packet `src` 一致，然后把该玩家加入 `BattleRoom`。

### MoveMessage

`GAME_MOVE` 的 `game_payload` 固定 2 bytes：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `x` | `i8` | 横向输入，通常为 `-1`、`0`、`1` |
| 2 | `y` | `i8` | 纵向输入，通常为 `-1`、`0`、`1` |

V 在 tick 中将输入向量归一化后乘以 `kTankSpeed`，再处理障碍物碰撞、坦克互相推开和世界边界 clamp。

### TargetMessage

`GAME_TARGET` 的 `game_payload`：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `angle` | `f32` | 炮塔角度，单位为度 |

V 保存该角度，后续开火时按该角度生成子弹目标点。

### ShootMessage

`GAME_SHOOT` 的 `game_payload`：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `shooting` | `bool_u8` | `1` 表示按住开火，`0` 表示停止开火 |

V 在 tick 中检查玩家是否正在 shooting，且不处于 reload 状态。如果满足条件，则创建子弹并进入 reload。

### NameMessage

`GAME_NAME` 的 `game_payload`：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `name` | `string_u8` | 新玩家名 |

### BattleStateSnapshot

`GAME_STATE` 的 `game_payload` 是完整世界快照：

| 顺序 | 字段 | 类型 | 说明 |
| --- | --- | --- | --- |
| 1 | `server_time_ms` | `u64` | V 生成快照时的服务器时间 |
| 2 | `total_score` | `u16` | 全局总分 |
| 3 | `winner_team` | `i8` | 当前获胜队伍；`-1` 表示无获胜展示 |
| 4 | `team_count` | `u8` | 队伍数量 |
| 5 | `teams[]` | repeated `TeamSnapshot` | 队伍状态 |
| 6 | `tank_count` | `u8` | 坦克数量 |
| 7 | `tanks[]` | repeated `TankSnapshot` | 坦克状态 |
| 8 | `bullet_count` | `u16` | 子弹数量 |
| 9 | `bullets[]` | repeated `BulletSnapshot` | 子弹状态 |
| 10 | `pickable_count` | `u16` | 补给数量 |
| 11 | `pickables[]` | repeated `PickableSnapshot` | 补给状态 |

`TeamSnapshot`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `team_id` | `u8` | 队伍编号 |
| `score` | `u16` | 队伍得分 |
| `tanks` | `u8` | 队伍中的坦克数量 |

`TankSnapshot`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `client_id` | `u8` | 所属 Client |
| `name` | `string_u8` | 玩家名 |
| `team` | `u8` | 队伍编号 |
| `x` | `f32` | 世界坐标 x |
| `y` | `f32` | 世界坐标 y |
| `angle` | `f32` | 炮塔角度 |
| `hp` | `i8` | 生命值 |
| `shield` | `i8` | 护盾值 |
| `dead` | `bool_u8` | 是否死亡 |
| `score` | `u16` | 个人得分 |

`BulletSnapshot`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `u16` | 子弹 ID |
| `owner_client_id` | `u8` | 发射者 Client |
| `x` | `f32` | 子弹坐标 x |
| `y` | `f32` | 子弹坐标 y |
| `special` | `bool_u8` | 是否为强化子弹 |

`PickableSnapshot`：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `u16` | 补给 ID |
| `type` | `u8` | `1=repair`, `2=damage`, `3=shield` |
| `x` | `f32` | 补给坐标 x |
| `y` | `f32` | 补给坐标 y |

### 游戏世界规则

V 的 `BattleRoom` 是权威状态。Client 只上传输入，最终位置、生命值、子弹、补给和分数都以 V 广播的 `GAME_STATE` 为准。

当前关键参数：

| 参数 | 值 | 说明 |
| --- | --- | --- |
| `kServerTickIntervalMs` | `20` | V 每 20ms 更新一次世界 |
| `kTankSpeed` | `0.2F * (20 / 33)` | 按旧 33ms tick 的移动节奏缩放 |
| `kBulletSpeed` | `0.65F * (20 / 33)` | 按旧 33ms tick 的子弹速度缩放 |
| `kTankRange` | `32.0F` | 子弹射程 32 个世界单位 |
| 世界大小 | `48 x 48` | `BattleRoom` 固定世界尺寸 |
| `kBulletDamage` | `3` | 普通子弹伤害 |
| `kReloadTimeMs` | `400` | 开火后 reload 时间 |
| `kRespawnTimeMs` | `5000` | 死亡后复活时间 |
| `kInvulnerableTimeMs` | `2000` | 复活后无敌窗口 |
| `kWinScore` | `10` | 队伍达到该分数触发胜利展示 |

子弹生成时根据坦克当前位置和炮塔角度计算起点与目标点。目标点为坦克位置沿角度方向延伸 `kTankRange`。子弹会在到达目标点、离开世界边界、撞到障碍物或命中敌方坦克时消失。

补给类型包括 repair、damage、shield。repair 恢复生命值，damage 提供强化弹药，shield 增加护盾。强化子弹造成更高伤害。

## 12. Client 与 Web UI 设计

Web UI 分为 Game 和 Protocol 两个主要视图。Game 视图用于登录、加入游戏和渲染坦克大战；Protocol 视图用于查看当前机器进程发出和收到的协议包。

### 登录状态

浏览器把 Client ID 和真实密码通过本地 WebSocket 发给 `client.exe`。Client 侧主要状态为：

| 状态 | 说明 |
| --- | --- |
| `waiting_for_login` | 等待浏览器提交 Client ID 和密码 |
| `authenticating` | 正在执行 AS/TGS/V_AUTH/CERT |
| `authenticated` | 完整认证成功，具备 `Kc_v`、V socket、双方公钥 |
| `joined` | 已发送 `GAME_JOIN_REQ` 并进入游戏状态 |

只有 AS/TGS/V_AUTH/CERT 全部成功后，UI 才进入可加入游戏状态。旧文档中“AS 阶段完成即可进入下一界面”的描述不符合当前实现。

### UI 到游戏链路

用户输入并不直接修改世界状态。输入路径为：

```text
Browser input -> local WebSocket -> client.exe -> signed/encrypted MSG_APP -> V
```

V 返回状态路径为：

```text
V GAME_STATE -> client.exe verify/decrypt/ACK -> local WebSocket -> Browser render
```

浏览器本地负责即时输入采集、画布渲染和界面切换；权威游戏状态来自 V。玩家看到方向键和炮塔转动响应较快，是因为浏览器渲染循环和本地输入采集开销低，但坦克最终位置、子弹、命中、死亡、分数都由 V 的 `GAME_STATE` 决定。

### V socket 复用

在 `--game-auth-encrypted` 模式下，Client 完成 V_AUTH 后不会关闭 V socket。后续 `CERT_C2V`、`CERT_V2C` 和所有游戏 `MSG_APP` 都复用同一条 V TCP 连接。这样避免了认证成功后重新连接导致的会话状态丢失，也让 `Kc_v` 和证书上下文与连接绑定。

### Client 并发边界

Client 中浏览器命令处理和 V 接收循环都可能向 V socket 写数据：浏览器命令会触发游戏输入发送，V 接收循环会在收到非 ACK 报文后发送 `APP_ACK`。因此 Client 使用 `send_mutex` 保护 V socket 写入，避免多个线程交错写同一条 TCP 连接。

## 13. Protocol Monitor 与报文可视化

协议可视化链路为：

```text
role process -> logs/protocol_events/*.txt -> monitor.exe -> protocolEvent JSON -> Browser Protocol panel
```

### protocolEvent 字段

每个 `protocolEvent` JSON 包含：

| 字段 | 来源 | 说明 |
| --- | --- | --- |
| `id` | monitor 分配 | UI 内部 packet 序号 |
| `timestamp` | `protocol_timestamp_now()` | 事件时间 |
| `role` | 日志文件和写日志角色 | 本机写出该事件的角色 |
| `direction` | send/recv 包装函数 | `SEND` 或 `RECV` |
| `endpoint` | Packet `src->dst` | 端点方向 |
| `message` | Packet + 解密视图 | 例如 `MSG_AS_REQ`、`MSG_APP.GAME_MOVE` |
| `category` | 消息分类 | `kerberos`、`app` 或 `error` |
| `header.msgType` | Packet header | 标签和值，如 `MSG_APP` / `0x66` |
| `header.src` | Packet header | 源实体标签和值 |
| `header.dst` | Packet header | 目的实体标签和值 |
| `header.payloadLen` | Packet header | payload 长度十进制和十六进制 |
| `header.reserved` | Packet header | 保留字段十进制和十六进制 |
| `payloadHex` | Packet payload | 网络上实际 payload hex |
| `payloadPlainHex` | payload view | 解密后的 payload 明文 hex |
| `payloadEncryptedHex` | payload view | 加密后的 payload 密文 hex |
| `payloadFields[]` | payload view | 结构化子字段的明文/密文 hex |

### UI 展示逻辑

Protocol 面板左侧显示 packet 列表，可按 All、Client、AS、TGS、V 过滤。高频包默认可隐藏，主要包括 `MSG_APP.GAME_STATE`、`MSG_APP.APP_ACK` 和 `MSG_APP.GAME_TARGET`。Pause 按钮用于暂停 UI 接收和滚动，便于观察某个已选 packet。

右侧展示三层信息：

1. Header：按固定 11B header 展示 `MSG_TYPE`、`SRC`、`DST`、`PAYLOAD_LEN`、`RESERVED`。
2. Payload：结构化解析 payload，例如 Kerberos 票据字段、认证器字段、签名应用层字段、ACK 引用字段。
3. Payload Hex：展示明文 payload hex 与加密 payload hex。SEND 时一般先展示明文再展示加密上网；RECV 时一般先展示收到的密文再展示解密后的明文。

### Kerberos 子字段可视化

Kerberos 阶段有些报文不是整体加密，但内部字段是加密的。例如：

| 报文 | 子字段 |
| --- | --- |
| `MSG_AS_REP` | `ticket_tgs` |
| `MSG_TGS_REQ` | `ticket_tgs`, `authenticator_tgs` |
| `MSG_TGS_REP` | `ticket_v` |
| `MSG_V_AUTH_REQ` | `ticket_v`, `authenticator_v` |

日志系统可以为这些字段写入 `fieldN_name`、`fieldN_plain_hex`、`fieldN_encrypted_hex`。Protocol UI 因此能在同一个 packet 下展示外层 payload 的明文/密文，以及内部票据或认证器字段的密文。

### 游戏 payload 可视化

游戏 `MSG_APP` 的 wire payload 是加密的 `SignedAppPayload`。收发方在本地拥有 `Kc_v`，因此日志可以同时写：

```text
payload_hex=<wire payload>
payload_plain_hex=<decrypted SignedAppPayload>
payload_encrypted_hex=<wire payload>
```

UI 使用 `payloadPlainHex` 解析 `SignedAppPayload`，再进一步解析 `GameMessage` 或 `AppAckPayload`，所以能够展示 `app_code`、`game_msg`、`signature`、`acked_payload_hash` 等结构化字段。

## 14. 日志设计

当前运行日志位于 `logs/` 下：

```text
logs/
  as.log
  tgs.log
  v_game.log
  client_game.log
  v_ack.log
  client_ack.log

  protocol_events/
    as_xxx.txt
    tgs_xxx.txt
    v_xxx.txt
    client_xx_xxx.txt

  runtime/
    as_server.out
    as_server.err
    tgs_server.out
    tgs_server.err
    v_server.out
    v_server.err
    client.out
    client.err
    monitor.out
    monitor.err
```

### 普通运行日志

| 路径 | 说明 |
| --- | --- |
| `logs/as.log` | AS 的连接、收发包、认证状态和错误 |
| `logs/tgs.log` | TGS 的连接、收发包、票据处理和错误 |
| `logs/v_game.log` | V 的 V_AUTH、证书交换、游戏输入、广播和错误 |
| `logs/client_game.log` | Client 的 UI 命令、认证流程、游戏发送、状态接收和错误 |

普通日志行格式为：

```text
[Entity][ThreadName][Event] message
```

### 不可否认证据日志

| 路径 | 说明 |
| --- | --- |
| `logs/v_ack.log` | V 验证 Client ACK 后写入的证据 |
| `logs/client_ack.log` | Client 验证 V ACK 后写入的证据 |

核心事件名为 `APP_NON_REPUDIATION_ACK`，message 中包含完整 ACK packet hex。

### 协议事件日志

`logs/protocol_events/*.txt` 是 Protocol Monitor 的输入源。每个本机角色进程会写自己的 protocol event 文件。该日志保留文本日志格式，同时 message 部分是 key-value：

```text
ts=... direction=SEND endpoint=Client1->V message=MSG_APP.GAME_MOVE category=app ...
```

它面向 UI 结构化展示，不是普通运行问题排查的唯一入口。

### runtime stdout/stderr

`logs/runtime/*.out` 和 `logs/runtime/*.err` 是启动脚本重定向的 stdout/stderr。它们用于排查进程启动失败、端口占用、依赖缺失等问题，不直接显示在 Protocol 面板中。

## 15. 线程模型与并发边界

### AS/TGS

AS 和 TGS 是 request/response TCP server。主线程监听端口，每接受一个连接后派发 worker thread 处理该连接上的认证请求，处理完成后关闭连接。它们不维护长期游戏状态，主要共享配置和认证运行时状态。

### V

V 是长连接游戏服务器。它包含：

| 线程/结构 | 责任 |
| --- | --- |
| accept loop | 接受 Client TCP 连接 |
| client worker thread | 对单个 socket 做 V_AUTH/CERT、收游戏报文、验签、ACK、应用输入 |
| game loop thread | 每 20ms tick 更新 `BattleRoom` 并广播 `GAME_STATE` |
| `connections_mutex_` | 保护已认证连接表 |
| `BattleRoom` mutex | 保护世界状态 |

V 的广播在 game loop 中完成。对每个连接，V 使用该连接保存的 `Kc_v` 和 Client 公钥构造签名加密 `GAME_STATE`。

### Client

Client 包含：

| 线程/结构 | 责任 |
| --- | --- |
| UI bridge | 接收浏览器 WebSocket 命令，广播登录状态和游戏状态 |
| login/auth path | 执行 AS/TGS/V_AUTH/CERT |
| receive loop | 持续读取 V 发来的 `MSG_APP` |
| `send_mutex` | 保护 V socket 写入 |
| `state_mutex` | 保护 Client 本地状态 |

receive loop 收到 V 的 `GAME_STATE` 后，会验签、解密、发送 ACK，并把 `BattleStateSnapshot` 转成 UI JSON 广播给浏览器。

### monitor.exe

monitor.exe 主线程接受浏览器 WebSocket 连接。每个连接由一个 handler 周期性 tail `logs/protocol_events/*.txt`，解析新增行，去重、排序后发送 `protocolEvent` JSON。tail 周期当前为 150ms。

### Logger

Logger 是异步批量写入的线程安全 line logger。业务线程调用 `write()` 只是把日志加入队列；writer thread 以短周期 flush 到文件，降低业务线程直接写文件的阻塞影响。

## 16. 性能测试与运行参数

四 Client 无浏览器压测脚本为：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\run_perf_4clients.ps1 -DurationSeconds 60 -InputHz 10
```

该脚本会构建需要的目标，在随机 localhost 端口启动隔离的 AS/TGS/V 进程，然后运行四个 headless authenticated clients。压测覆盖 Kerberos 认证、V_AUTH、证书交换、签名加密游戏输入、V 广播 `GAME_STATE`、双向 `APP_ACK` 等链路，不包含浏览器渲染成本。

最近一次 20ms tick 的短时压测记录显示：

```text
clients=4
avg_state_interval_ms=20.00
max_state_interval_ms=22.38
errors=0
```

之前 60 秒压测也观察到平均状态间隔为 20.00ms，最大状态间隔约 24.46ms，错误数为 0。当前主要压力来自协议事件日志增长，而不是 V 的 CPU。5 秒压测中 V 侧 protocol event 日志增长约 451KB/s，四个 Client 的 protocol event 日志各约 114KB/s。

报告中解释性能时应区分三件事：

1. V game loop tick：当前目标是 20ms。
2. 网络和协议链路：压测覆盖签名、加密、ACK 和状态广播。
3. 浏览器渲染：压测不包含浏览器成本，需要通过真实 UI 演示观察。

## 17. 旧文档中已推翻或需修正的设计

- 旧文档中“AS 阶段完成即可进入下一界面”的表述不符合当前实现。当前 UI 在完整 AS/TGS/V_AUTH/CERT 成功后才进入可加入游戏状态。
- 旧文档中“等待所有 Client 后由 V 发送 GAME_START”的设计不作为当前最终行为。当前实现中玩家 join 后即可由 V 的权威世界状态驱动游戏。
- 旧文档中“Client 直接读取本机 AS/TGS/V 普通日志并展示”的设计已被替换为 `monitor.exe` 读取 `logs/protocol_events/*.txt`。
- 旧文档中应用层明文游戏报文不是最终运行模式。最终运行模式是 `--game-auth-encrypted`，游戏 `MSG_APP` payload 签名后加密。
- 旧文档中若出现 33ms tick 或 16 单位子弹射程，需要改为当前 20ms tick 和 32 单位子弹射程。
- 旧文档中若有浏览器直接连接 V 的图，应改为浏览器连接本地 `client.exe`，由 `client.exe` 连接 V。

## 18. 可复用图片建议

可基本复用的图片包括：仍与当前界面一致的登录和游戏 UI 截图；只表达“固定首部 + 变长数据部分”概念的通用报文格式图；不涉及具体旧链路的 Kerberos 角色图。

复用前需要重审的图片包括：Kerberos 流程图、部署图、日志展示图和报文可视化图。Kerberos 图必须体现 AS -> TGS -> V 的顺序，且不能暗示浏览器直接连 V。部署图必须体现每台玩家机器上本地浏览器连接本地 `client.exe` 和本地 `monitor.exe`。日志图必须体现 `monitor.exe` 从 `logs/protocol_events/*.txt` 读取协议事件，而不是 Client 直接读取 AS/TGS/V 普通日志。

建议重画的图片包括：

- 全系统链路图：浏览器、本地 client、AS/TGS/V、monitor、logs 的关系。
- Kerberos 字段图：每阶段 payload、密钥和加密边界。
- 游戏 `MSG_APP` 封装图：Packet header、加密 payload、SignedAppPayload、GameMessage。
- 双向 ACK 不可否认图：原始报文、ACK 引用字段、证据日志。
- Protocol Monitor 数据流图：role process -> protocol_events -> monitor -> browser。

旧 docx 的嵌入图片已提取到：

```text
E:\zhuomian\cybersecurity\课程设计\report_assets\old_docx_media
```

图片使用索引位于：

```text
E:\zhuomian\cybersecurity\课程设计\report_assets\old_docx_image_index.md
```

## 19. 写最终报告时建议展开的内容

最终报告建议围绕当前实现展开，不再把历史上的明文模式、旧 `GAME_START` 流程或浏览器直连 V 作为主线。

建议报告主线：

1. 系统整体架构：浏览器、本地 Client bridge、AS/TGS/V、Monitor 的边界。
2. 通用报文格式：11B header、payload、字节序和长度前缀。
3. Kerberos 完整认证：`Kc`、`Kc_tgs`、`Kc_v`、`Ticket_tgs`、`Ticket_v` 和三个阶段 payload。
4. 证书交换：Client/V 公钥如何进入运行时，如何用于应用层签名验证。
5. 应用层安全封装：固定 header 不变，`SignedAppPayload` 签名后加密。
6. 双向不可否认：非 ACK 游戏报文必须返回签名加密 `APP_ACK`，ACK 完整 packet 写入证据日志。
7. 坦克大战应用层：V 权威状态、Client 输入上报、20ms tick、32 单位子弹射程、状态广播。
8. 报文可视化：Protocol panel 如何展示 header、结构化 payload、明文 hex、密文 hex 和方向元信息。
9. 运行与性能：单机启动、四机部署、四 Client 加密链路压测、日志增长对 UI 观察的影响。

报告中可以少写伪代码，多用字段表、流程图、报文字段图和 UI 截图说明当前系统。涉及旧 docx 图片时，先按第 17 节逐项检查是否与最终实现一致。
